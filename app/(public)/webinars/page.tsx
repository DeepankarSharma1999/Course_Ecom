import { notFound } from "next/navigation";

// Placeholder page hidden until real webinar content exists.
// Restore the old version from git history when it's ready.
export default function WebinarsPage() {
  notFound();
}
